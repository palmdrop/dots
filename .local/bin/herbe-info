#!/bin/bash

vol="$(vol print)%"

if [ "$(pamixer --get-mute)" = "true" ]
then
    vol="(Muted) $vol"
fi

bat="$(bat)%"

killall herbe & 
herbe \
      "$(date +'%A, %d %h, %H:%M')" \
      "_______________________" " " \
      "Volume: $vol" " "  \
      "Charge: $bat"  \

